<% @roles.each do |role| %>
  <b><%=link_to role.name, role %></b>
  <%=h role.description %>
  <%= link_to(pluralize(role.calls.count, 'call'), role_calls_path(role)) %><br>
<% end %>


<br />

<%= link_to 'add role', new_role_path %>